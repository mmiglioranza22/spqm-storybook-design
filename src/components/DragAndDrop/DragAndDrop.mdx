import { Meta, Title, Primary, Controls } from "@storybook/blocks";
import * as DragAndDropStories from "./DragAndDrop.stories";

<Meta of={DragAndDropStories} />

<Title>Drag and dop example</Title>

This component relies on `@dnd-kit` and only exposes drag and drop functionalities. Check the API reference on [dnd-kit website](https://docs.dndkit.com/).

<Primary />

## Controls

<Controls />
