import { Meta, Title, Primary, Controls } from "@storybook/blocks";
import * as RadarStories from "./Radar.stories";

<Meta of={RadarStories} />

<Title>Radar example</Title>

This component relies on `recharts` [library](https://recharts.org/en-US).

Check the API reference on [recharts website](https://recharts.org/en-US/api/RadarChart).

<Primary />

## Controls

<Controls />
