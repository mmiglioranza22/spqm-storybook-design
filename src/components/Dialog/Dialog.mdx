import { Meta, Title, Primary, Controls, AddContext } from "@storybook/blocks";
import * as DialogStories from "./Dialog.stories";

<Meta of={DialogStories} />

<Title>Dialog example</Title>

This component exposes only the required API to enable the open/close dialog
function with the respective backdrop. See full API reference available on [Radix website](https://www.radix-ui.com/primitives/docs/components/dialog#api-reference).

Consumers can extend it and nest other dialog related components from `@radix-ui/react-dialog`

<Primary />
